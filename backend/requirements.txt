# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

# --- Agents / LLM ---
crewai>=0.30
crewai-tools>=0.4
openai>=1.30

# --- State / messaging ---
redis>=5.0

# --- Observability ---
weave>=0.51
wandb>=0.17

# --- CopilotKit runtime bridge ---
# TODO [Person 2 / Person 4]: confirm the correct Python CopilotKit package + version
copilotkit>=0.1

# --- Utils ---
python-dotenv>=1.0
httpx>=0.27
numpy>=1.26
pillow>=10.0          # photo encode/decode for the vision pipeline

# --- Edge / Gemma (hardware mode; optional in sim) ---
# TODO [Person 3]: pin the Gemma 2B runtime you choose (ollama client, llama-cpp-python,
#   transformers, etc.) and the Lego SPIKE/Mindstorms SDK.
# ollama>=0.2
# pybricks  # or the relevant Lego API
